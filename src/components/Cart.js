import React from "react";

function Cart() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: "column",
        background: "#ececec",
        width: "20%",
        margin: 10,
        padding: 10,
      }}
    >
      Cart
    </div>
  );
}

export default Cart;
